# Per-repo values for the shared ../devtools scripts (see
# ../devtools/templates/devtools.conf.sample for every key).
REPO_NAME=fdk-aac-prebuilt
CI_ENV_PREFIX=FDK_AAC_PREBUILT
WINCI_STACK=rust
WINCI_EXTRA_EXCLUDES="./build ./crates/*/prebuilt ./audio ./e2e-out"
